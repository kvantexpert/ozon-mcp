# План массовой загрузки каталога 1С в Ozon

## Цель

Загрузить **293 позиции 1С** из GOODS.JSON в новый аккаунт Ozon.

Источник: GOODS.JSON — 31 982 предложения.

**GOODS.JSON не изменять.**

## Текущий статус — 24.09.2026

Классификация целевого каталога завершена.

В PowerShell получено:

`CATALOG_293=293`

Точный набор определяется **только группами 1С**:

- `ИДГруппы=00000002` — 193 позиции
- `ИДГруппы=00000003` — 100 позиций
- Итого: **293**

Дополнительные группы, ранее попавшие в расширенный набор 327, исключены:

- `00000292` — 28
- `00000296` — 4
- `00000314` — 2

То есть 327 − 34 = **293**.

Повторная фильтрация по названиям НЕ нужна.

## Новый аккаунт Ozon

Сервер MCP:

`https://ozon-mcp.kvantexpert.ru/mcp`

Новые Client ID + API key уже установлены на сервере в окружении сервиса.

Проверка авторизации:

- `ready=true`
- источник ключей: `env`
- активный кабинет определяется окружением
- ошибок отсутствующих ключей нет

Лимиты нового аккаунта:

- всего товаров: **0 / 500**
- дневное создание: **0 / 1500**
- дневное обновление: **0 / 20000**
- rate limit: 30000/мин

293 позиции помещаются в лимит 500.

## Категория Ozon

Рабочая категория:

- `description_category_id=200001489` — Цифровые товары
- `type_id=971075562` — Код активации офисного приложения

Обязательный атрибут категории:

- `8229` — Тип

Другие проверенные характеристики категории:

- 85 — Бренд
- 9048 — Название модели (для объединения в одну карточку)
- 6574 — Номинал
- 9297 — Срок действия
- 9300 — Тип носителя
- 11469 — Язык
- 5162 — Платформа
- 77 — Разработчик
- 4381 — Артикул
- 9024 — Код продавца
- и другие

Важно: `6574 Номинал` — числовой атрибут. Его нельзя использовать для значений Базовая/ПРОФ/КОРП.

## Эталонная карточка

Проверенная карточка Ozon:

- product_id: `6417979753`
- offer_id: `4601546116680`
- sku: `5865629857`
- название: `1С:Бухгалтерия 8 ПРОФ. Электронная поставка`
- category: `200001489`
- type_id: `971075562`

Фактически проверенные атрибуты эталона сохранены в истории проекта.

## Контракт импорта — подтверждён 24.09.2026

Через текущий MCP-сервер выполнено:

`ozon_search_methods("product import")`

Получен реальный endpoint:

- operation_id: `ozon_product_import`
- method: `POST`
- host: `api-seller.ozon.ru`
- path: `/v3/product/import`
- safety: `write`
- максимум: **100 товаров за запрос**
- режим: **асинхронный**, возвращает `task_id`

Через `ozon_describe_method("ozon_product_import")` подтверждён контракт:

`body: {items:[{offer_id, attributes, images, dimensions, type_id}]}`

Для отправки через MCP используется `ozon_write_method` с:

- `operation_id="ozon_product_import"`
- `body` согласно контракту
- `confirm_write=true`

Статус задачи проверяется отдельным read endpoint:

- operation_id: `ozon_product_import_info`
- method: `POST`
- path: `/v1/product/import/info`

**Важно:** контракт endpoint подтверждён, но значения полей конкретных 293 товаров ещё не сформированы и не отправлялись.

## Важный вывод по вариантам

Базовая / ПРОФ / КОРП, электронная / коробочная, количество пользователей и другие варианты являются отдельными предложениями из GOODS.JSON и **не должны сливаться в один товар при подготовке 293 позиций**.

## Что уже сделано

1. Создан и сохранён план массового импорта.
2. GOODS.JSON разобран без изменения исходного файла.
3. Найден полный набор 31 982 предложений.
4. Определён точный набор 293 позиций через группы `00000002` + `00000003`.
5. Проверены категория и type_id Ozon.
6. Проверены атрибуты эталонной карточки.
7. Старый Ozon-аккаунт был заполнен 500 товарами и затем все 500 были архивированы; архивирование лимит 500 не освободило.
8. Для дальнейшей загрузки переключены ключи MCP на новый Ozon-аккаунт.
9. Новый аккаунт проверен: 0/500 товаров.
10. Найден реальный endpoint импорта.
11. Получен и зафиксирован реальный контракт `ozon_product_import`.
12. Подтверждён endpoint проверки `ozon_product_import_info`.

## Что НЕ делать

- Не изменять GOODS.JSON.
- Не возвращаться к фильтрации 293 по названиям.
- Не загружать старый набор Dr.Web.
- Не использовать старый аккаунт для массовой загрузки.
- Не угадывать dictionary_value_id для атрибута 8229.
- Не считать карточку созданной только по отправке запроса: нужен подтверждённый результат Ozon.
- Не отправлять все 293 до формирования и проверки payload.

## Следующий шаг

Сформировать из `$catalog293` **первую партию максимум 100 items** в точном формате:

`{items:[{offer_id, attributes, images, dimensions, type_id}]}`

Перед отправкой проверить данные партии локально и только затем выполнить `ozon_write_method`.

После каждой партии:

1. сохранить `task_id`;
2. вызвать `ozon_product_import_info`;
3. зафиксировать результат;
4. перейти к следующей партии.

## Формула партий

293 товара = 100 + 100 + 93.

Целевой результат: **293 позиции загружены в новый аккаунт Ozon**.


## Зафиксированная задача для продолжения

Главная задача: **загрузить все 293 позиции** из групп `00000002` + `00000003` в новый Ozon-аккаунт.

Не устраивать отдельный предварительный ручной цикл проверки всех 293. Работать партиями **100 + 100 + 93** через `POST /v3/product/import`, после каждой партии проверять только фактический результат через `POST /v1/product/import/info` и исправлять конкретные ошибки Ozon.

### Что прочитать перед продолжением

1. `docs/MASS_CATALOG_IMPORT_PLAN.md`
2. `docs/PRICE_RULE_293.md`
3. `server/VPS_RUNTIME.md`
4. `server/UPSTREAM_PIN.md`

После чтения этих файлов не спрашивать пользователя повторно про цель, количество 293, источник GOODS.JSON, лимиты, правило цены и endpoint импорта.

GitHub issue задачи: #2 — «Загрузить 293 позиции 1С в Ozon — без отдельного цикла предварительных проверок».


## Аудит и фактический блокер — 24.09.2026

После сверки с актуальной схемой Ozon API уточнён контракт импорта. Сокращённое описание MCP operation `{offer_id, attributes, images, dimensions, type_id}` не является полной схемой Ozon Seller API.

Для реального `POST /v3/product/import` нужно учитывать как минимум:

- `offer_id`;
- `description_category_id`;
- `type_id`;
- `price`;
- `currency_code`;
- `depth`, `width`, `height`, `dimension_unit`;
- `weight`, `weight_unit`;
- `attributes`;
- изображения при наличии.

Ozon требует реальные ненулевые объёмно-весовые характеристики. Поле `dimensions` не следует считать самостоятельным полем Ozon API.

### Текущий фактический статус нового аккаунта

24.09.2026:

- `ozon_product_list`: **0 товаров**;
- `/v4/product/info/limit`: total **0/500**, daily_create **2/1500**, daily_update **0/20000**, rate **30000/min**;
- `/v1/description-category/tree`: `200001489` и `971075562` возвращаются с `disabled=true`;
- импорт без `description_category_id`: `description_category_is_empty`;
- импорт с `description_category_id=200001489`: `used_forbidden_category`.

**Главный блокер сейчас — доступность категории в новом Ozon-кабинете.** Лимит 500 и дневной лимит создания 1500 не являются блокером.

Не повторять массовый импорт до снятия этого блокера.

### Исправление прежних формулировок

- `200001489` больше не считать «рабочей категорией» для нового аккаунта; считать её эталонной/исследованной категорией, пока API возвращает `disabled=true`.
- `daily_create=0/1500` заменить на последний фактический снимок `2/1500`.
- `active_cabinet` из `ozon_check_auth` фактически был `null`, `cabinets=[]`; не описывать named cabinet как подтверждённый.
- Не путать MCP wrapper description с полной Ozon request schema.

Полный аудит сохранён в `docs/AUDIT_2026-09-24.md`.
